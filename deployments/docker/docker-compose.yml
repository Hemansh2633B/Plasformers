services:
  plasformers:
    build:
      context: ../..
      dockerfile: deployments/docker/Dockerfile
    image: plasformers:latest
    ports:
      - "8000:8000"
    environment:
      - PLAS_CACHE=/models
    volumes:
      - ../../checkpoints:/models
