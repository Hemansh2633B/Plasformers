# Android Deployment

Recommended path:

1. `plas export tflite --variant nano --int8`
2. Calibrate with device-specific images.
3. Package the `.tflite` model in an NNAPI-enabled app.

For Qualcomm or MediaTek NPUs, use the vendor compiler from the ONNX seed
artifact generated by `plas export onnx`.
